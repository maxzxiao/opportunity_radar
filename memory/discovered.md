# Discovered programs

Written by the Opportunity Radar routine on the `claude/discoveries` branch.
Each morning's run reads this to avoid re-surfacing things it already found,
then appends whatever is genuinely new.

Do not hand-edit on `main` — the routine only ever reads and writes the copy on
`claude/discoveries`. To promote something into the permanently tracked roster,
move it into `programs.yaml` instead.

| Program | URL | First seen | Last status | Last checked |
|---|---|---|---|---|
